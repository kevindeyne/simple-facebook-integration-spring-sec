package com.redleafbooks.page;

import org.springframework.mobile.device.Device;

public abstract class AbstractController {

	protected boolean isDesktopUser(Device device) {
		return device == null || device.isNormal();
	}
}
